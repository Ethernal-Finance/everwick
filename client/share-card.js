export function saveNewsCard(canvas,edition){
 const card=document.createElement('canvas');card.width=1200;card.height=630;const c=card.getContext('2d');c.fillStyle='#132b21';c.fillRect(0,0,1200,630);c.drawImage(canvas,650,0,550,630);c.fillStyle='#132b21ef';c.fillRect(0,0,720,630);c.fillStyle='#dfbd73';c.font='22px Georgia';c.fillText('✦  EVERWICK',55,65);c.font='14px sans-serif';c.fillText(`THE GLOB GAZETTE  /  DAY ${edition.day}`,55,130);c.fillStyle='#f2ecd4';c.font='bold 43px Georgia';let y=210,line='';for(const word of edition.headline.split(' ')){const next=line?line+' '+word:word;if(c.measureText(next).width>600){c.fillText(line,55,y);y+=54;line=word;}else line=next;}c.fillText(line,55,y);c.fillStyle='#b9c4a9';c.font='19px sans-serif';c.fillText('A story from a town that keeps living.',55,500);c.font='16px sans-serif';c.fillText(location.host,55,548);c.font='12px sans-serif';c.fillText('Development alpha · Fantasy Dreamland art © ElvGames',55,592);card.toBlob(blob=>{if(!blob)return;const a=document.createElement('a'),url=URL.createObjectURL(blob);a.href=url;a.download=`everwick-gazette-day-${edition.day}.png`;a.click();setTimeout(()=>URL.revokeObjectURL(url),10000);});
}
